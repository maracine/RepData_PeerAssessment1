data <- read.csv("activity.csv")
filtered_data <- na.omit(data)
setwd("/Users/Michel/Dropbox/Coursera/Data Science - John Hopkin's/Reproducible Research/week2/Course Project 1")
data <- read.csv("activity.csv")
filtered_data <- na.omit(data)
library(dplyr)
library(ggplot2)
library("ggplot2")
total_steps_per_day <- aggregate(steps ~ date, data, sum)
mean_steps <- mean(total_steps_per_day$steps)
median_steps <- median(total_steps_per_day$steps)
qplot(total_steps_per_day$steps, binwidth=1000, xlab="total number of steps taken each day")
steps_per_interval <- aggregate(steps ~ interval, data, mean)
plot(steps_per_interval$interval, steps_per_interval$steps, type="l", xlab="Interval",
ylab="Average number of steps")
Max_steps <- max(steps_per_interval$steps)
Max_interval <- steps_per_interval[steps_per_interval$steps == Max_steps,]$interval
missing_values = sum(!complete.cases(data))
data2 <- transform(data, steps = ifelse(is.na(data$steps), steps_per_interval$steps[match(data$interval, steps_per_interval$interval)], data$steps))
total_steps_per_day2 <- aggregate(steps ~ date, data2, sum)
mean_steps2 <- mean(total_steps_per_day2$steps)
median_steps2 <- median(total_steps_per_day2$steps)
qplot(total_steps_per_day2$steps, binwidth=1000, xlab="total number of steps taken each day")
missing_values = sum(!complete.cases(data))
data2 <- transform(data, steps = ifelse(is.na(data$steps), steps_per_interval$steps[match(data$interval, steps_per_interval$interval)], data$steps))
total_steps_per_day2 <- aggregate(steps ~ date, data2, sum)
mean_steps2 <- mean(total_steps_per_day2$steps)
median_steps2 <- median(total_steps_per_day2$steps)
plot(total_steps_per_day2$steps, binwidth=1000, xlab="total number of steps taken each day")
missing_values = sum(!complete.cases(data))
data2 <- transform(data, steps = ifelse(is.na(data$steps), steps_per_interval$steps[match(data$interval, steps_per_interval$interval)], data$steps))
total_steps_per_day2 <- aggregate(steps ~ date, data2, sum)
mean_steps2 <- mean(total_steps_per_day2$steps)
median_steps2 <- median(total_steps_per_day2$steps)
plot(total_steps_per_day2$steps, binwidth=1000, xlab="total number of steps taken each day", plot="l")
missing_values = sum(!complete.cases(data))
data2 <- transform(data, steps = ifelse(is.na(data$steps), steps_per_interval$steps[match(data$interval, steps_per_interval$interval)], data$steps))
total_steps_per_day2 <- aggregate(steps ~ date, data2, sum)
mean_steps2 <- mean(total_steps_per_day2$steps)
median_steps2 <- median(total_steps_per_day2$steps)
warnings(plot(total_steps_per_day2$steps, binwidth=1000, xlab="total number of steps taken each day", plot="l"))
missing_values = sum(!complete.cases(data))
data2 <- transform(data, steps = ifelse(is.na(data$steps), steps_per_interval$steps[match(data$interval, steps_per_interval$interval)], data$steps))
total_steps_per_day2 <- aggregate(steps ~ date, data2, sum)
mean_steps2 <- mean(total_steps_per_day2$steps)
median_steps2 <- median(total_steps_per_day2$steps)
plot(total_steps_per_day2$steps, binwidth=1000, xlab="total number of steps taken each day", type="l")
missing_values = sum(!complete.cases(data))
data2 <- transform(data, steps = ifelse(is.na(data$steps), steps_per_interval$steps[match(data$interval, steps_per_interval$interval)], data$steps))
total_steps_per_day2 <- aggregate(steps ~ date, data2, sum)
mean_steps2 <- mean(total_steps_per_day2$steps)
median_steps2 <- median(total_steps_per_day2$steps)
plot(total_steps_per_day2$steps, xlab="total number of steps taken each day", type="l")
missing_values = sum(!complete.cases(data))
data2 <- transform(data, steps = ifelse(is.na(data$steps), steps_per_interval$steps[match(data$interval, steps_per_interval$interval)], data$steps))
total_steps_per_day2 <- aggregate(steps ~ date, data2, sum)
mean_steps2 <- mean(total_steps_per_day2$steps)
median_steps2 <- median(total_steps_per_day2$steps)
qplot(total_steps_per_day2$steps, binwidth=1000, xlab="total number of steps taken each day")
plot(steps_per_interval$interval, steps_per_interval$steps, type="l", xlab="Interval",
ylab="Average number of steps")
qplot(total_steps_per_day$steps, binwidth=1000, xlab="total number of steps taken each day")
missing_values = sum(!complete.cases(data))
data2 <- transform(data, steps = ifelse(is.na(data$steps), steps_per_interval$steps[match(data$interval, steps_per_interval$interval)], data$steps))
total_steps_per_day2 <- aggregate(steps ~ date, data2, sum)
mean_steps2 <- mean(total_steps_per_day2$steps)
median_steps2 <- median(total_steps_per_day2$steps)
qplot(total_steps_per_day2$steps, binwidth=1000, xlab="total number of steps taken each day")
qplot(total_steps_per_day$steps, binwidth=1000, xlab="total number of steps taken each day")
qplot(total_steps_per_day2$steps, binwidth=1000, xlab="total number of steps taken each day, imputing missing values with mean of 5-minute intervals")
library(lubridate)
install.packages(lubridate)
install.packages("lubridate")
library(lubridate)
library(lubridate)
date
date()
isweekday(date())
weekday(date())
weekdays(date())
weekdays(date())
wday(date())
install.packages("chron")
library(chron)
is.weekend(date())
dates("21/03/2018")
dates("03/21/2018")
is.weekend(dates("03/21/2018"))
ifelse(is.weekend(data.i$date), "Weekend", "Weekday")
data.i <- transform(data, steps = ifelse(is.na(data$steps), steps_per_interval$steps[match(data$interval, steps_per_interval$interval)], data$steps))
ifelse(is.weekend(data.i$date), "Weekend", "Weekday")
is.weekend(data.i$date)
data.i$date
View(data.i)
View(data.i)
data.i$weekday <- ifelse(is.weekend(data.i$date), "Weekend", "Weekday")
library(lattice)
xyplot(steps_per_interval_i$steps ~ steps_by_interval_i$interval|steps_by_interval_i$dow, main="Average Steps per Day by Interval",xlab="Interval", ylab="Steps",layout=c(1,2), type="l")
steps_per_interval.i <- aggregate(steps ~ interval, data.i, mean)
xyplot(steps_per_interval.ii$steps ~ steps_per_interval.i$interval|steps_per_interval.i$dow, main="Average Steps per Day by Interval",xlab="Interval", ylab="Steps",layout=c(1,2), type="l")
xyplot(steps_per_interval.i$steps ~ steps_per_interval.i$interval|steps_per_interval.i$dow, main="Average Steps per Day by Interval",xlab="Interval", ylab="Steps",layout=c(1,2), type="l")
steps_per_interval.i <- aggregate(steps ~ interval + weekday, dat
steps_per_interval.i <- aggregate(steps ~ interval + weekday, data.i, mean)
View(steps_per_interval)
View(steps_per_interval.i)
xyplot(steps_per_interval.i$steps ~ steps_per_interval.i$interval|steps_per_interval.i$weekday, main="Average Steps per Day by Interval",xlab="Interval", ylab="Steps",layout=c(1,2), type="l")
qplot(total_steps_per_day$steps, binwidth=1000, xlab="Total number of steps taken each day", main="mean total number of steps taken per day")
qplot(total_steps_per_day.i$steps, binwidth=1000, xlab="Number of steps per day", main="Total number of steps taken each day - imputing missing values with mean of 5-minute intervals")
total_steps_per_day.i <- aggregate(steps ~ date, data.i, sum)
qplot(total_steps_per_day.i$steps, binwidth=1000, xlab="Number of steps per day", main="Total number of steps taken each day - imputing missing values with mean of 5-minute intervals")
